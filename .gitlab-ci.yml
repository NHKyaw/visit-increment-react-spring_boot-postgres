stages:
  - build


default:
  image: docker:24.0.5-cli
  services:
    - docker:24.0.5-dind
  before_script:
    - docker info


build:
  stage: build
  tags:
    - DinD
  script:
    - apk add node
    - node --version
    - apk add npm
    - npm --version

