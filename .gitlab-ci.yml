stages:
  - test
  - build
  - deploy

test:
  stage: test
  image: eclipse-temurin:21-jdk
  services:
    - name: postgres:15
      alias: postgres
      command: ["postgres", "-c", "listen_addresses='*'"]
      variables:
          # Service configuration (GitLab passes these to the Postgres container)
          POSTGRES_DB: my_database
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: "12345678"
  script:
    - echo "Running backend tests..."
    - cd backend
    - ./mvnw test

